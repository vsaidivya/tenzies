import React from "react";

export default function Header(){
    return (
        <div className="Head"  >
            <h1 >Tenzies</h1>
            <p>Roll and lock the dice until you get a same number on every dice</p>
        </div>
    )
}